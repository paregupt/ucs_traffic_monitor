# Add new domain in format:IP_Address,username,password
# You can (and should) use DNS name instead of IP address as long as the name resolution works correctly
# Add comma between items
# Do not leave whitespaces between items  or empty lines
# Do not leave an empty line
# Lines starting with # are ignored
# Text between [] is location. At least one location is mandatory.
[US]
#192.168.1.1,admin,passwd
#[UK]
#192.168.1.1,admin,passwd
#[Italy]
#192.168.1.1,admin,passwd
#[China]
#192.168.1.1,admin,passwd
